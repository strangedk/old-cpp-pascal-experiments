{	Программу составил Чуприна В.Е.-преподаватель кафедрры
	информационных технологий ХОНМИНО, тел.23-27-93. }
	Uses Crt;
   Label FinitaLaCommedia;
   Var x0,y0,x1,y1,eps,a,b,c,d,r:extended;
			       s:char;
Begin ClrScr;
WriteLn('Не ограничивая общности, рассмотрим круг радиуса R с центром в начале координат!');
Write('Введите радиус круга R='); ReadLn(R);
Write('Введите абциссу внутренней точки круга х0='); ReadLn(x0);
Write('Введите ординату внутренней точки круга y0='); ReadLn(y0);
WriteLn('Для удобства выбора точки на окружности абциссу будем определять вводом, а      ординату вычислять!');
Write('Введите абциссу точки окружности х1='); ReadLn(x1);
       y1:=-Sqrt(R*R-x1*x1);
WriteLn('Выберем точку на верхней полуокружности? Да - Y, нет - N.');
       ReadLn(s);  if ((s='Y') or (s='y')) then y1:=-y1;
WriteLn('Ордината точки окружноcти y1=',y1:13:11); WriteLn;
		       { Начало алгоритма! }
	 a:=y1-y0; b:=x0-x1; c:=y0*x1-x0*y1; d:=Abs(c/Sqrt(a*a+b*b));
   if Abs(d)<1E-33 then begin
WriteLn('Луч света пройдет через центр круга до или после первого отражения от окружномти');
		     goto FinitaLaCommedia end
       else
WriteLn('Луч света будет проходить мимо центра круга на минимальном расстоянии');
WriteLn;
WriteLn('                           d=',d:13:11,' !');
FinitaLaCommedia:
WriteLn('Press Enter'); ReadLn;
End.
